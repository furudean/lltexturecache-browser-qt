"""The list of textures, and how it is filled

The model behind the grid, the queue that decides what to decode next, the
stores the panes decode into, and the small pieces that read a selection back
off the view.
"""
